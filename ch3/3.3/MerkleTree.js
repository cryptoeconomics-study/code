class MerkleTree {
    constructor(leaves, concat) {
        this.leaves = leaves;
        this.concat = concat;
    }
    reduceLayers(layer) {
    }
    getProof(index, layer = this.leaves, proof = []) {
    }
    getRoot() {
    }
}

module.exports = MerkleTree;
